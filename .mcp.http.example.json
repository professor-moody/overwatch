{
  "//": "Reference only. `npm run setup` writes the real URL and token, then `npm run daemon:start` starts the one shared owner. Stdio mode (.mcp.example.json) is the explicit solo/no-subagent fallback.",
  "mcpServers": {
    "overwatch": {
      "type": "http",
      "url": "http://127.0.0.1:3000/mcp",
      "headers": {
        "Authorization": "Bearer ${OVERWATCH_MCP_TOKEN}"
      }
    }
  }
}
